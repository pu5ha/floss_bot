# Copy to .env and fill in. Only Telegram milestones (M8+) need these.
# The single network credential is the Telegram bot token — no LLM/API keys.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
CONTACT_EMAIL=
